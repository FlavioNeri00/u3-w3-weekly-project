const initialState = {
  content: ["queen", "u2", "thepolice", "eagles", "thedoors", "oasis", "thewho", "bonjovi"],
};

const rockArtistsReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
export default rockArtistsReducer;
