const initialState = {
  content: ["maroon5", "coldplay", "onerepublic", "jamesblunt", "katyperry", "arianagrande"],
};

const popArtistsReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default popArtistsReducer;
